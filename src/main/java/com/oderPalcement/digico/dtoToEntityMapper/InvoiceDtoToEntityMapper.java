package com.oderPalcement.digico.dtoToEntityMapper;

import com.oderPalcement.digico.dto.InvoiceDto;
import com.oderPalcement.digico.entity.Invoice;
import com.oderPalcement.digico.entity.User;

public class InvoiceDtoToEntityMapper {

/*
    public static Invoice getInvoiceEntity(InvoiceDto invoiceDto, Invoice invoice){

        invoice.setId(invoice.getId() !=null? invoice.getId() : invoiceDto.getId());
        invoice.setName(invoiceDto.getName());
        invoice.setQuantity(invoiceDto.getQuantity());
        invoice.setTotal(invoiceDto.getTotal());
        invoice.setSubTotal(invoiceDto.getSubTotal());
        invoice.setMemberId(invoiceDto.getMemberId());

        return invoice;


    }*/



}
